import * as React from 'react';

import { TodoList } from '../components/TodoList';

interface CompletedScreenProps {
  
}

export const CompletedScreen: React.FC<CompletedScreenProps> = ({ children, ...props }) => {
  return (<></>);
}